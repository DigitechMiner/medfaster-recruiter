/**
 * Chat wire shapes — expand when backend contracts are finalized.
 * Conversations/messages are normalized to arrays at the API layer.
 */
export type ChatConversationRow = unknown;
export type ChatMessageRow = unknown;
